const express = require('express');
const router = express.Router();
const userController = require('../controllers/userController');

// Agrega aquí las rutas para login, register, etc.

module.exports = router;
